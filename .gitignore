# Build artifacts
target/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Rust / Cargo
# For binary projects, Cargo.lock is generated and should be ignored.
# For library projects, commit Cargo.lock so builds are reproducible.
Cargo.lock

# Debug / profiling
*.rs.bk

# Coverage reports
coverage/
tarpaulin-report*

# Local development overrides
local.properties

#playwright
.playwright-mcp

# Lima sandbox VM images (disks are disposable; source images are not committed)
dev/sandbox/lima/images/**/*.qcow2
dev/sandbox/lima/images/**/*.img
dev/sandbox/lima/images/**/*.raw
dev/sandbox/lima/images/**/*.iso

# Lima sandbox artifacts (collected test output)
.sandbox-artifacts/

log.txt
